# ROS 2 <-> Gazebo Harmonic bridge config for v1 (Tier 2).
# Used by `ros_gz_bridge parameter_bridge --config-file`.
#
# With Tier 2, ros2_control handles ALL joint commands and joint-state
# publishing internally (via the gz_ros2_control plugin running inside
# Gazebo), so the bridge no longer carries:
#   - per-joint /arm/*_cmd or /gripper/*_cmd topics  (moved to JTC)
#   - /joint_states                                  (joint_state_broadcaster)
#   - /grasp/{attach,detach}                         (friction grasp, no joint)
#
# Only the diff-drive base and the sim clock still cross the bridge.

# Sim clock: Gazebo -> ROS
- ros_topic_name: "/clock"
  gz_topic_name: "/clock"
  ros_type_name: "rosgraph_msgs/msg/Clock"
  gz_type_name: "gz.msgs.Clock"
  direction: GZ_TO_ROS

# Velocity commands: ROS -> Gazebo (driven by gz-sim DiffDrive plugin)
- ros_topic_name: "/cmd_vel"
  gz_topic_name: "/model/pos_robot/cmd_vel"
  ros_type_name: "geometry_msgs/msg/Twist"
  gz_type_name: "gz.msgs.Twist"
  direction: ROS_TO_GZ

# Odometry: Gazebo -> ROS
- ros_topic_name: "/odom"
  gz_topic_name: "/model/pos_robot/odometry"
  ros_type_name: "nav_msgs/msg/Odometry"
  gz_type_name: "gz.msgs.Odometry"
  direction: GZ_TO_ROS
