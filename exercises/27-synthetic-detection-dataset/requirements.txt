# Python dependencies for exercise 27 (synthetic detection dataset).
# Install with:  pip install -r requirements.txt
#
# numpy  -> array math, random generators, mask arithmetic.
# pillow -> rasterise the procedural scene to an RGB image.
numpy>=1.24
pillow>=10.0
